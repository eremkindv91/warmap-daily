{
  "name": "warmap-daily",
  "version": "1.0.0",
  "description": "Daily control-line and OSINT territorial change map based on verifiable open sources.",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "node -e \"console.log('Build complete')\"",
    "start": "node server.js",
    "lint": "node --check assets/app.js && node --check server.js"
  }
}
